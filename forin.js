var colors = {
    primary : 'Blue',
    secondary : 'Red',
    tertiary : 'White'
}

for (var color in colors){
    console.log(color + ' : '+colors[color])
}

var colorsArray = ['Orange', 'Green', 'yellow', 'Pink']

for (var color in colorsArray){
    console.log(color + ' : '+colorsArray[color])
}